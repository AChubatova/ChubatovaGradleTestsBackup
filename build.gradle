plugins {
    id("java")
    id("org.gradle.test-retry") 
}  
repositories {
    mavenCentral()
}

dependencies {
    testImplementation("junit:junit:4.13")
}

tasks.test {
    useJUnit()
    retry {
        maxRetries.set(30)
        maxFailures = 20
        failOnPassedAfterRetry = false
    }
}
